<%# add navigation links to this file %>
<li><%= link_to (t'nav_links.about'), page_path('about') %></li>
<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= (t'category.categories') %> <span class="caret"></span></a>
  <ul class="dropdown-menu">
    <li><%= link_to (t'category.all_cat'), categories_path %></li>
    <% Category.all.each do |category| %>
      <% if params[:locale] == "en" %>
        <li><%= link_to category.name, category_path(category) %></li>
      <% else %>
        <li><%= link_to category.categoria, category_path(category) %></li>
      <% end %>
    <% end %>
      <%# if logged_in? and current_user.admin? %>
        <li role="separator" class="divider"></li>
        <li><%= link_to (t'category.new_cat'), new_category_path %></li>
      <%# end %>
  </ul>
</li>
<%# link_to 'Search', search_path('search') %>
<%# link_to "Agencies", agencies_path %>
<li><%= link_to "English", locale: "en" %></li>
<li><%= link_to "Español", locale: "es" %></li>
