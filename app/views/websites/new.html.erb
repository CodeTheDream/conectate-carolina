</br>
<%= form_for @website, html: {class: 'form-horizontal'}, wrapper: :horizontal_form do |f| %>
  <% if not @website.nil? and @website.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@website.errors.count, "error") %>
      prohibited this organization from being saved:</h2>
    <ul>
      <% @website.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <%= f.text_field :socialmedia, placeholder: "URL here" %>
  <%= f.submit (t'buttons_t.save'),class: 'btn btn-primary' %>
<% end %>