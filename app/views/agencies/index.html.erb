<script src="//maps.google.com/maps/api/js?key=AIzaSyCj5WxwF7qYcjlvsjtzabyVi0Se1WtuA6w"></script>
<script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
<script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script>
<!-- only if you need custom infoboxes -->
<!-- Script is used to connect to Google API, This nees to be moved to a better location
 since it has been copied in index and show view -JR -->

<script>

var x = document.getElementById("location");
function getLocation() {
    if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(showPosition);
    } else {
        x.innerHTML = "Geolocation is not supported by this browser.";
    }
}
function showPosition(position) {
	var x = document.getElementById("coordinates");
    x.value = position.coords.latitude + "," + position.coords.longitude;
    console.log(x.value)
}
getLocation();
</script>
<section class="top-background" id="farm-background">
  <div class="header">

  <h1><%= (t'home_page.title') %></h1>
  <h2><%= (t'home_page.browse') %></h2>
  <section class="index-search">
    <div class="wrapper">
      <%= form_tag agencies_path(anchor: "organizations"), method: :get, class: 'search-form', id: 'agency-lookup-form', data: { behaviors: 'turboform' } do %>

        <%= text_field_tag :search, params[:search], placeholder: (t'search_page.plchldr_search'), :class => 'form-control search-input', autofocus: params[:search].present? %>

        <!-- <%= text_field_tag :location, params[:location], placeholder: "search by location", :class => 'form-control search-input', autofocus: params[:location].present? %> -->
        <% search_spinner = "<i class='fa fa-spinner fa-pulse fa-lg fa-fw'></i> Searching..." %>
        <span class="input-group-btn">
          <%= button_tag( type: "submit", class: "btn btn-success", :name => nil,
                          data: {disable_with: search_spinner }) do %>
            <span class="glyphicon glyphicon-search"></span>
            <%= (t'search_page.bttn_search') %>
          <% end %>
        </span>
      <% end %>
    </div>
  </section>
</section>
</div>

<hr>

<div class="row row-centered">
  <%= render 'sidebar' %>
  <!-- end col-md-3 sidebar-->
  <%= render 'agencies' %>
</div>
<%#end row %>
